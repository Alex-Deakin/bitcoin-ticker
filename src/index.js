import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import BitcoinWidget from './BitcoinWidget';

// Check out BitcoinWidget.js for all of the main code
ReactDOM.render(<BitcoinWidget />, document.getElementById('root'));